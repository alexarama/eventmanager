spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/eventmanager
    username: postgres
    password: FMI2026
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  thymeleaf:
    cache: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: aleexarama@gmail.com
    password: lkwb apga xzic cpsv
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

logging:
  level:
    com.example.eventmanager: DEBUG
    org.hibernate.SQL: DEBUG

server:
  port: 8080